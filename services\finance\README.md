# Finance services

Esta camada contÃ©m apenas os pontos de extensÃ£o do domÃ­nio financeiro. Os serviÃ§os sÃ£o stubs intencionalmente vazios nesta sprint: nÃ£o possuem acesso a banco, Supabase, CRUD ou efeitos colaterais.

As regras de negÃ³cio devem ser adicionadas aqui, enquanto os contratos de entrada permanecem em `features/*/schemas` e as entidades em `types/finance.ts`.

