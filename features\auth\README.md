# Auth

Responsável pelos fluxos de login, cadastro, recuperação e redefinição de senha. A autenticação usa exclusivamente Supabase Auth; senhas nunca são persistidas pela aplicação. Schemas, serviços, hooks e componentes permanecem isolados neste módulo.
